import PasswordCompare from "@/app/components/PasswordCompare";

export default function PasswordTestPage() {
  return (
    <div className="min-h-screen bg-gray-100 py-10">
      <PasswordCompare />
    </div>
  );
}
